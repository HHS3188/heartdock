# AI 协作交接状态

## 当前项目状态

- 项目名称：HeartDock
- 技术栈：Electron + React + TypeScript + Vite
- 当前准备版本：v0.5.1
- 当前发布物：已从当前分支重新生成 `release\HeartDock Setup 0.5.1.exe`
- 当前阶段：#48 / #54 当前体验已获用户确认，文档已同步，安装包已重新打包
- 当前主线原则：先稳定后功能

## 当前协作模式

- ChatGPT 主线：负责规划、审查、解释、发布把关
- Claude Code：负责本地执行、读源码、改文件、跑命令、检查 diff
- 用户：负责最终确认、操作授权、体验判断

## 当前 open issue

- #48：鼠标穿透关闭时偶发退出（本地已修复/缓解，等待 commit / PR / release 流程）
- #54：增加交互锁定模式（本地已并入为鼠标禁止交互模式，等待 commit / PR / release 流程）
- #41：支持自定义心率背景图片（暂缓，建议放到 v0.6.0）
- #52：统一 ChatGPT 与 Claude Code 对接流程（已通过 #53 落地）

## 当前推荐下一步

1. 验证新的 `release\HeartDock Setup 0.5.1.exe` 安装包。
2. 安装包验证通过后再 commit / push / PR / release。
3. 发布前确认 GitHub Release 使用的是本次重新生成的安装包。
4. 当前原则：不发布旧安装包，先稳定后功能。

## 当前已知风险

- 禁止交互模式已通过用户体验确认，但重新打包后的安装包仍需要最终安装验证。
- BLE 心率设备仍为实验性功能。
- 透明窗口、鼠标穿透、多显示器、高 DPI 环境可能存在兼容差异。

## 修改安全规则

- 每次改代码前确认 git status。
- 每次改代码前记录基线提交 SHA。
- 大改动前说明影响范围。
- 影响已有正常功能时，优先回退到修改前正常状态。
- 未经用户确认，不执行破坏性 git 操作。

## 交接总结规则

- 默认简短：优先 5-8 行说清做了什么、改了什么、检查结果、Git 状态、下一步。
- 大改动、出错、高风险、准备 commit/push/release 时，必须输出详细阶段总结。
- Claude Code 自行判断详细程度，不强制固定模板。

## 最近一次同步

- 来源：Codex 接手本地修复分支
- 目标：同步用户确认 #48 / #54 当前体验正常，完成文档同步和重新打包
- 结果：`npm run dist` 已成功生成新的 `release\HeartDock Setup 0.5.1.exe`，等待安装包验证和 Git 流程
